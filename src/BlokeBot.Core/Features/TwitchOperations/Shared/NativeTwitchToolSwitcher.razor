<nav class="studio-segmented max-w-full flex-wrap" aria-label="Native Twitch tools">
    @if (_shoutouts)
    {
    <NavLink class="studio-segmented__option text-center no-underline" ActiveClass="bg-[var(--app-surface-solid)] text-[var(--app-text-strong)] shadow-[var(--app-shadow-sm)]" Match="NavLinkMatch.All" href="twitch-operations/shoutouts">Shoutouts</NavLink>
    }
    @if (_polls)
    {
    <NavLink class="studio-segmented__option text-center no-underline" ActiveClass="bg-[var(--app-surface-solid)] text-[var(--app-text-strong)] shadow-[var(--app-shadow-sm)]" Match="NavLinkMatch.All" href="twitch-operations/polls">Polls</NavLink>
    }
    @if (_clips)
    {
    <NavLink class="studio-segmented__option text-center no-underline" ActiveClass="bg-[var(--app-surface-solid)] text-[var(--app-text-strong)] shadow-[var(--app-shadow-sm)]" Match="NavLinkMatch.All" href="twitch-operations/clips-markers">Clips &amp; markers</NavLink>
    }
    @if (_rewards)
    {
    <NavLink class="studio-segmented__option text-center no-underline" ActiveClass="bg-[var(--app-surface-solid)] text-[var(--app-text-strong)] shadow-[var(--app-shadow-sm)]" Match="NavLinkMatch.All" href="twitch-operations/channel-points">Rewards &amp; redemptions</NavLink>
    }
    @if (_predictions)
    {
    <NavLink class="studio-segmented__option text-center no-underline" ActiveClass="bg-[var(--app-surface-solid)] text-[var(--app-text-strong)] shadow-[var(--app-shadow-sm)]" Match="NavLinkMatch.All" href="twitch-operations/predictions">Predictions</NavLink>
    }
</nav>
