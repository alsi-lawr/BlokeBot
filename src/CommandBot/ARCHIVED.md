# Archived

`src/CommandBot` is archived and is not part of the active BlokeBot solution boundary.

It remains in the repository for reference only. Do not update it as part of BlokeBot, `Alsi.TwitchBot`, or `BlokeBot.Persistence` work unless the project is explicitly reactivated.

Active work uses `CommandBot.slnx`, which includes `src/Alsi.TwitchBot`, `src/BlokeBot.Persistence`, `src/BlokeBot`, and `tests`. Self-contained references inside this directory, such as its project file or Dockerfile, belong to the archive and are not active build inputs.
