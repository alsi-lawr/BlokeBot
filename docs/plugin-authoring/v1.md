# BlokeBot plugin author reference v1

This reference is generated from the canonical plugin contracts. Regenerate the author artifacts instead of editing its contract tables.

## Runtime contract

| Contract | Version |
| --- | ---: |
| Manifest | 1 |
| Package policy | 1 |
| Typed value | 1 |
| Host API | 1 |
| Lua | 5.4 |

Plugins are fully trusted Lua 5.4 packages with the full standard library. The worker process limits availability failures; it is not a security boundary. Do not describe a plugin as sandboxed.

## Identity and submission

Use one stable lowercase plugin ID. Contract IDs are 1-64 characters, start with a lowercase ASCII letter, end with a lowercase letter or digit, and may contain non-adjacent `.`, `-`, or `_` separators.

A marketplace submission names the manifest's declared semantic version and one mutable Git tag. Do not submit or record a commit SHA as plugin identity.

## Package and targets

The package root contains `blokebot.plugin.json`. Declare every Lua module, browser or media asset, and other payload. Paths are relative canonical `/` paths. Absolute paths, `.` or `..` segments, links, undeclared files, missing declared files, and exact or case-folded collisions are rejected.

Every asset and payload declares its path, purpose, maximum size, and supported runtime identifiers. Compatibility requires the selected runtime identifier on every declaration. Payloads may be native files, .NET assemblies, WebAssembly, or another plugin-managed type. Admission enforces declarations, targets, paths, links, collisions, and byte limits; it does not infer or police a payload's byte type from its extension.

Supported runtime identifiers:

- `linux-x64`
- `linux-arm64`
- `osx-arm64`
- `win-x64`
- `win-arm64`

Lua modules are the only BlokeBot-managed entrypoints. Other payloads and their dependencies remain the trusted plugin's responsibility.

## Manifest shapes

JSON names and shapes below come from the canonical `PluginManifest` graph. JSON is camel-case, rejects unknown or duplicate members, and uses the listed discriminator values for variant records.

### `PluginManifest`

| JSON member | Type |
| --- | --- |
| `manifestVersion` | number |
| `id` | contract identifier string |
| `name` | string |
| `description` | string |
| `release` | `PluginReleaseIdentity` |
| `compatibility` | `PluginCompatibilityDeclaration` |
| `entryModule` | contract identifier string |
| `luaModules` | `PluginLuaModuleDescriptor` array |
| `assets` | `PluginAssetDescriptor` array |
| `payloads` | `PluginPayloadDescriptor` array |
| `settings` | `PluginSettingDescriptor` array |
| `features` | `PluginFeatureDescriptor` array |
| `hostModules` | `PluginHostModuleRequirement` array |
| `migrations` | `PluginMigrationDescriptor` array |
| `automationDefinitions` | `PluginAutomationDefinitionDescriptor` array |
| `automationTemplates` | `PluginAutomationTemplateDescriptor` array |
| `generatedPages` | `PluginGeneratedPageDescriptor` array |
| `embeddedPages` | `PluginEmbeddedPageDescriptor` array |

### `PluginActionDescriptor`

| JSON member | Type |
| --- | --- |
| `id` | contract identifier string |
| `module` | contract identifier string |
| `operation` | contract identifier string |
| `requirements` | `PluginCallbackRequirements` |

### `PluginAssetDescriptor`

| JSON member | Type |
| --- | --- |
| `id` | contract identifier string |
| `path` | string |
| `kind` | `PluginAssetKind` |
| `mediaType` | string |
| `purpose` | string |
| `runtimeIdentifiers` | `PluginRuntimeIdentifier` array |
| `maximumBytes` | number |

### `PluginAssetKind`

Values: `browser`, `media`

### `PluginAutomationDefinitionDescriptor`

| JSON member | Type |
| --- | --- |
| `id` | contract identifier string |
| `featureId` | contract identifier string |
| `kind` | `PluginAutomationDefinitionKind` |
| `name` | string |
| `description` | string |
| `module` | contract identifier string |
| `entryPoint` | string |
| `inputs` | `PluginAutomationFieldDescriptor` array |
| `outputs` | `PluginAutomationFieldDescriptor` array |

### `PluginAutomationDefinitionKind`

Values: `source`, `action`, `value`, `control`, `transform`

### `PluginAutomationFieldDescriptor`

| JSON member | Type |
| --- | --- |
| `id` | contract identifier string |
| `name` | string |
| `valueKind` | `PluginValueKind` |
| `required` | boolean |

### `PluginAutomationTemplateDescriptor`

| JSON member | Type |
| --- | --- |
| `id` | contract identifier string |
| `featureId` | contract identifier string |
| `name` | string |
| `nodes` | `PluginAutomationTemplateNode` array |
| `edges` | `PluginAutomationTemplateEdge` array |

### `PluginAutomationTemplateEdge`

| JSON member | Type |
| --- | --- |
| `fromNode` | contract identifier string |
| `fromOutput` | contract identifier string |
| `toNode` | contract identifier string |
| `toInput` | contract identifier string |

### `PluginAutomationTemplateNode`

| JSON member | Type |
| --- | --- |
| `id` | contract identifier string |
| `definitionId` | contract identifier string |
| `configuration` | `PluginValue` |

### `PluginBlokeBotEventKind`

Values: `hostedChannelsChanged`, `guessingChanged`, `pointsChanged`, `overlaysChanged`, `twitchOperationsChanged`

### `PluginCallbackRequirements`

| JSON member | Type |
| --- | --- |
| `twitchReady` | boolean |

### `PluginCommandDescriptor`

| JSON member | Type |
| --- | --- |
| `route` | string |
| `module` | contract identifier string |
| `operation` | contract identifier string |
| `requirements` | `PluginCallbackRequirements` |

### `PluginCompatibilityDeclaration`

| JSON member | Type |
| --- | --- |
| `minimumApiVersion` | API version number |
| `maximumApiVersion` | API version number |
| `minimumBlokeBotVersion` | semantic version string |
| `maximumBlokeBotVersionExclusive` | semantic version string |
| `luaVersion` | `PluginLuaVersion` |

### `PluginDispatchDeclarations`

| JSON member | Type |
| --- | --- |
| `commands` | `PluginCommandDescriptor` array |
| `events` | `PluginEventHandlerDescriptor` array |
| `schedules` | `PluginScheduleHandlerDescriptor` array |
| `webhooks` | `PluginWebhookDescriptor` array |
| `actions` | `PluginActionDescriptor` array |

### `PluginEmbeddedPageDescriptor`

| JSON member | Type |
| --- | --- |
| `id` | contract identifier string |
| `featureId` | contract identifier string |
| `route` | string |
| `title` | string |
| `documentAsset` | contract identifier string |
| `assets` | contract identifier string array |
| `messageOrigins` | absolute URI array |

### `PluginEventHandlerDescriptor`

| JSON member | Type |
| --- | --- |
| `id` | contract identifier string |
| `source` | `PluginEventSource` |
| `module` | contract identifier string |
| `operation` | contract identifier string |
| `requirements` | `PluginCallbackRequirements` |

### `PluginEventSource`

Variants: `twitch` -> `PluginEventSource.Twitch`, `twitchRaw` -> `PluginEventSource.TwitchRaw`, `blokeBot` -> `PluginEventSource.BlokeBot`

### `PluginEventSource.BlokeBot`

| JSON member | Type |
| --- | --- |
| `kind` | `PluginBlokeBotEventKind` |

### `PluginEventSource.Twitch`

| JSON member | Type |
| --- | --- |
| `kind` | `PluginTwitchEventKind` |

### `PluginEventSource.TwitchRaw`

| JSON member | Type |
| --- | --- |
| `eventSubType` | string |
| `version` | string |

### `PluginFeatureDescriptor`

| JSON member | Type |
| --- | --- |
| `id` | contract identifier string |
| `name` | string |
| `description` | string |
| `settings` | contract identifier string array |
| `twitch` | `PluginTwitchRequirements` |
| `automationTemplates` | contract identifier string array |
| `dispatch` | `PluginDispatchDeclarations` or null |

### `PluginGeneratedPageDescriptor`

| JSON member | Type |
| --- | --- |
| `id` | contract identifier string |
| `featureId` | contract identifier string |
| `route` | string |
| `title` | string |
| `module` | contract identifier string |
| `renderEntryPoint` | string |

### `PluginHostModuleRequirement`

| JSON member | Type |
| --- | --- |
| `id` | contract identifier string |
| `minimumVersion` | API version number |
| `maximumVersion` | API version number |

### `PluginLuaModuleDescriptor`

| JSON member | Type |
| --- | --- |
| `id` | contract identifier string |
| `path` | string |

### `PluginLuaVersion`

Values: `lua54`

### `PluginMigrationDescriptor`

| JSON member | Type |
| --- | --- |
| `id` | contract identifier string |
| `fromVersion` | semantic version string |
| `toVersion` | semantic version string |
| `module` | contract identifier string |
| `entryPoint` | string |

### `PluginPayloadDescriptor`

| JSON member | Type |
| --- | --- |
| `id` | contract identifier string |
| `path` | string |
| `purpose` | string |
| `runtimeIdentifiers` | `PluginRuntimeIdentifier` array |
| `maximumBytes` | number |

### `PluginReleaseIdentity`

| JSON member | Type |
| --- | --- |
| `declaredVersion` | semantic version string |
| `tag` | mutable Git tag string |

### `PluginRuntimeIdentifier`

Values: `linux-x64`, `linux-arm64`, `osx-arm64`, `win-x64`, `win-arm64`

### `PluginScheduleHandlerDescriptor`

| JSON member | Type |
| --- | --- |
| `id` | contract identifier string |
| `module` | contract identifier string |
| `operation` | contract identifier string |
| `requirements` | `PluginCallbackRequirements` |

### `PluginSettingChoiceDescriptor`

| JSON member | Type |
| --- | --- |
| `id` | contract identifier string |
| `name` | string |

### `PluginSettingDescriptor`

| JSON member | Type |
| --- | --- |
| `id` | contract identifier string |
| `name` | string |
| `description` | string |
| `scope` | `PluginSettingScope` |
| `required` | boolean |
| `schema` | `PluginSettingSchema` |

### `PluginSettingSchema`

Variants: `boolean` -> `PluginSettingSchema.Boolean`, `text` -> `PluginSettingSchema.Text`, `multilineText` -> `PluginSettingSchema.MultilineText`, `integer` -> `PluginSettingSchema.Integer`, `number` -> `PluginSettingSchema.Number`, `duration` -> `PluginSettingSchema.Duration`, `choice` -> `PluginSettingSchema.Choice`, `secret` -> `PluginSettingSchema.Secret`

### `PluginSettingSchema.Boolean`

| JSON member | Type |
| --- | --- |

### `PluginSettingSchema.Choice`

| JSON member | Type |
| --- | --- |
| `choices` | `PluginSettingChoiceDescriptor` array |

### `PluginSettingSchema.Duration`

| JSON member | Type |
| --- | --- |
| `minimumSeconds` | number |
| `maximumSeconds` | number |

### `PluginSettingSchema.Integer`

| JSON member | Type |
| --- | --- |
| `minimum` | number |
| `maximum` | number |

### `PluginSettingSchema.MultilineText`

| JSON member | Type |
| --- | --- |
| `maximumLength` | number |

### `PluginSettingSchema.Number`

| JSON member | Type |
| --- | --- |
| `minimum` | number |
| `maximum` | number |
| `decimalPlaces` | number |

### `PluginSettingSchema.Secret`

| JSON member | Type |
| --- | --- |
| `maximumLength` | number |

### `PluginSettingSchema.Text`

| JSON member | Type |
| --- | --- |
| `maximumLength` | number |

### `PluginSettingScope`

Values: `installation`, `channel`

### `PluginTwitchEventKind`

Values: `streamOnline`, `streamOffline`, `channelUpdated`, `followReceived`, `subscriptionReceived`, `subscriptionGiftReceived`, `cheerReceived`, `incomingRaidReceived`, `hypeTrainChanged`, `chatNotificationReceived`, `rewardRedemptionReceived`, `shoutoutOccurred`, `pollChanged`, `predictionChanged`

### `PluginTwitchRequirements`

| JSON member | Type |
| --- | --- |
| `scopes` | number array |
| `eventSubTypes` | number array |

### `PluginValue`

Variants: `nil` -> `PluginValue.Nil`, `boolean` -> `PluginValue.Boolean`, `number` -> `PluginValue.Number`, `string` -> `PluginValue.String`, `array` -> `PluginValue.Array`, `map` -> `PluginValue.Map`

### `PluginValue.Array`

| JSON member | Type |
| --- | --- |
| `items` | `PluginValue` array |

### `PluginValue.Boolean`

| JSON member | Type |
| --- | --- |
| `value` | boolean |

### `PluginValue.Map`

| JSON member | Type |
| --- | --- |
| `properties` | `PluginValueProperty` array |

### `PluginValue.Nil`

| JSON member | Type |
| --- | --- |

### `PluginValue.Number`

| JSON member | Type |
| --- | --- |
| `value` | number |

### `PluginValue.String`

| JSON member | Type |
| --- | --- |
| `value` | string |

### `PluginValueKind`

Values: `nil`, `boolean`, `number`, `string`, `array`, `map`

### `PluginValueProperty`

| JSON member | Type |
| --- | --- |
| `name` | string |
| `value` | `PluginValue` |

### `PluginWebhookAuthentication`

Variants: `public` -> `PluginWebhookAuthentication.Public`, `callback` -> `PluginWebhookAuthentication.Callback`

### `PluginWebhookAuthentication.Callback`

| JSON member | Type |
| --- | --- |
| `module` | contract identifier string |
| `operation` | contract identifier string |

### `PluginWebhookAuthentication.Public`

| JSON member | Type |
| --- | --- |

### `PluginWebhookDescriptor`

| JSON member | Type |
| --- | --- |
| `id` | contract identifier string |
| `module` | contract identifier string |
| `operation` | contract identifier string |
| `requirements` | `PluginCallbackRequirements` |
| `authentication` | `PluginWebhookAuthentication` |

## Host calls

Call `blokebot.host.call(module, operation, ...)`. Values are limited to nil, boolean, finite number, string, array, and string-keyed map. The generated Lua language-server stub supplies one overload per operation.

| Module | Operation | Contexts | Arguments | Result |
| --- | --- | --- | --- | --- |
| `diagnostics` | `log` | installation, channel, automation, migration, page | string, string | nil |
| `responses` | `chat` | channel | string | nil |
| `responses` | `whisper` | channel | string | nil |
| `chat` | `send` | channel | string | nil |
| `overlay` | `play-cue` | channel | string, string | nil |
| `points` | `add` | channel | string, string, string | string |
| `twitch` | `create-marker` | channel | string | nil |
| `schedules` | `once` | channel | string, string, map | string |
| `schedules` | `recurring` | channel | string, string, number, map | string |
| `schedules` | `cancel` | channel | string | nil |
| `storage` | `execute` | installation, channel, automation, migration, page | string, map | number |
| `storage` | `query` | installation, channel, automation, migration, page | string, map | array |
| `http` | `send` | installation, channel, automation, page | map | map |

Declare every host module your plugin uses with a minimum and maximum API version. Host failures and cancellations are typed outcomes. Treat safe failure messages as operator-readable detail, not provider internals.

## Invocations and effects

Features may declare settings, Twitch requirements, commands, event handlers, schedules, webhooks, actions, automation definitions and templates, generated pages, and embedded pages. A declaration does not register a live feature or run Lua.

Host-call waits are always cancellable. Cancellation stops the coroutine from waiting and a later result is not resumed. It does not promise to undo a chat message, schedule, HTTP request, SQLite write, or other effect that completed before cancellation won.

Use the `storage` module for plugin-private SQLite operations and `http.send` for approved web integrations. Generated and embedded UI pages must use declared modules or browser assets. Keep migrations deterministic and surface a typed update failure when migration Lua fails. A worker crash is isolated from the host and reported as a typed worker failure.

## Executable examples

Published sources live under `examples/plugins`. The Contracts test harness packages each example from local files, validates it for its declared runtime targets, and executes every declared scenario through the supported worker protocol. The harness has deterministic host adapters and does not install into the production local-source directory, join production inventory, contact Twitch, or make third-party network requests.

## Remove

Remove is destructive for plugin-owned state. It removes the installed package, installation and channel settings, feature state, configuration, secrets, schedules, private data, automation definitions, ledgers, dependent flows and nodes, run history, receipts, and invocation context. Global marketplace catalogue metadata remains available so the plugin can be discovered and installed again.
